<form method="post" action="{{ path('app_stadium_delete', {'id': stadium.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce stade ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stadium.id) }}">
    <button class="btn">Supprimer</button>
</form>
