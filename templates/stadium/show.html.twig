{% extends 'base.html.twig' %}

{% block title %}Stadium{% endblock %}

{% block body %}
    <h1>Stadium</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stadium.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ stadium.Name }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ stadium.City }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ stadium.Country }}</td>
            </tr>
            <tr>
                <th>Capacity</th>
                <td>{{ stadium.Capacity }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ stadium.photo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_stadium_index') }}">back to list</a>

    <a href="{{ path('app_stadium_edit', {'id': stadium.id}) }}">edit</a>

    {{ include('stadium/_delete_form.html.twig') }}
{% endblock %}
