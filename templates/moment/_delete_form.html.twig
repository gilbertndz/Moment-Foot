<form method="post" action="{{ path('app_moment_delete', {'id': moment.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce Moment ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ moment.id) }}">
    <button class="btn">Supprimer</button>
</form>
